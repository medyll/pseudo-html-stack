# Test Plan — pseudo-kit post-audit 2026-03-02

## Scope

Post-audit release verification. Covers all fixes applied on 2026-03-02 (AUDIT.md)
plus existing coverage gap analysis.

---

## Coverage Inventory

| Module | Test file | Runner | Tests | Status |
|---|---|---|---|---|
| `shared/registry-shared.js` | `tests/registry-shared.test.js` | node --test | 27 | ✅ green |
| `shared/state-shared.js` | `tests/state-shared.test.js` | node --test | 32 | ✅ green |
| `server/pseudo-kit-server.js` | `tests/pseudo-kit-server.test.js` | node --test | 41 | ✅ green |
| `client/pseudo-kit-client.js` | `tests/pseudo-kit-client.client.test.js` | vitest | ? | ⚠️ not run |
| `server/canvas-validator.js` | ❌ none | — | 0 | ❌ gap |
| `server/canvas-normalize.js` | ❌ none | — | 0 | ❌ gap |
| `pseudo-assets/index.js` | ❌ none | — | 0 | ❌ gap |

---

## Gaps & Priority

### P1 — Direct regressions from AUDIT fixes

| Fix | Module | Test needed |
|---|---|---|
| BUG-02 `_findInstances` uses `m.index` | `canvas-validator.js` | Same tag in two frames → distinct frameIds |
| BUG-03 `button` removed from LAYOUT_ELEMENTS | `canvas-validator.js` | `<button>` in frame → "not declared" error |
| BUG-05 `_parseAttrs` tag-name skip | `canvas-validator.js` | `<row row="x">` → `row` attr not skipped |
| NOM-06 5 files renamed to `-pk` | `pseudo-assets/index.js` | URLs resolve to existing `-pk.html` files |

### P2 — Module coverage gaps

| Module | Key behaviours to cover |
|---|---|
| `canvas-validator.js` | valid canvas passes, missing registry error, undeclared tag error, duplicate frameId bug regression |
| `canvas-normalize.js` | `_findBlock` returns `fullStart`/`fullEnd`, `_addMissingComponentRoles`, `_renameObsoleteAttrs` |

### P3 — Client (vitest)

Run `pnpm run test:client` and record result. No new tests needed unless failures found.

---

## New Test Files

| File | Runner | Covers |
|---|---|---|
| `tests/canvas-validator.test.js` | node --test | P1 BUG-02/03/05 + P2 validator |
| `tests/canvas-normalize.test.js` | node --test | P2 normalizer + BUG-04 typedef |
| `tests/pseudo-assets-index.test.js` | node --test | P1 NOM-06 URLs |

---

## Pass Criteria

- All `node --test` suites: 0 failures
- `pnpm run test:client` (vitest): 0 failures
- BUG-02 regression test: two identical tags in different frames → distinct frameIds
- NOM-06: `button`, `input`, `label`, `textarea`, `footer` URLs end with `-pk.html`
- `button` tag → reported as undeclared (not silently skipped as layout element)

---

## CI Note

Add to `package.json` once validated:

```json
"test:all": "pnpm test && pnpm run test:client"
```

Already updated to use `pnpm` (CFG-02 fix).
