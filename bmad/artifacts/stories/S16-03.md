# S16-03 — pseudo-kit-cli — npx pseudo-kit init project scaffold

**Epic:** DX
**Sprint:** Sprint 16
**Points:** 5
**Priority:** Could
**Status:** done

## User Story
As a new developer, I want to run `npx pseudo-kit init my-app` and get a ready-to-run project with pseudo-kit pre-wired, so that I can start building components without manual boilerplate.

## Implementation Notes

**Date:** 2026-03-11
**Files changed:**
- `bin/pseudo-kit-init.js` — standalone Node.js CLI script (new)
- `package.json` — added `"bin": { "pseudo-kit": "./bin/pseudo-kit-init.js" }`

**Generated scaffold:**
- `index.html` — entry page with `pseudo-kit-client.js` auto-loaded
- `demo.html` — demo page with button-pk, badge-pk, progress-pk, tabs-pk examples
- `components/` — empty folder for custom components
- `package.json` — minimal package.json with pseudo-stack dependency

**Idempotent:** already-existing files are skipped, not overwritten.
**Help:** `npx pseudo-kit --help` prints usage.
