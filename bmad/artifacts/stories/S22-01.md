# S22-01 — FIX-PKG-01: README + bin key → `pseudo-stack`

**Priority:** High  
**Points:** 2

## Problem

Root package name is `pseudo-stack` (correct), but README.md and bin key still reference `pseudo-html-kit` / `pseudo-kit`.

## Acceptance Criteria

1. `README.md` title: `# pseudo-stack`
2. `README.md` install snippet: `npm install pseudo-stack`
3. `README.md` CDN URL: `cdn.jsdelivr.net/npm/pseudo-stack/...`
4. `README.md` all `import ... from 'pseudo-html-kit'` → `import ... from 'pseudo-stack'`
5. `README.md` SSR example: `from 'pseudo-stack/server'`
6. `package.json` bin key: `"pseudo-stack": "./bin/pseudo-kit-init.js"` (keep script path, change key)
7. `README-full.md` — same replacements if it exists

## Files to touch

- `README.md`
- `package.json` (bin key only)
- `README-full.md` (if exists)
