# S16-01 — pseudo-kit-react unit tests — hooks + provider

**Epic:** DX
**Sprint:** Sprint 16
**Points:** 5
**Priority:** Must
**Status:** done

## User Story
As a contributor, I want the pseudo-kit-react adapter to have a full unit test suite, so that refactors and upgrades don't silently break the React integration.

## Definition of Done
- [x] 14 unit tests covering useComponent, usePseudoKit, usePseudoKitReady, useRegisterComponent, PseudoKitProvider
- [x] Tests run via vitest in jsdom environment with @vitejs/plugin-react JSX transform
- [x] Source renamed to `src/index.jsx` for correct JSX handling
- [x] Package version bumped to 0.3.0
- [x] Zero warnings in CI output

## Implementation Notes

**Date:** 2026-03-11
**Files changed:**
- `src/pseudo-kit-react/src/index.jsx` — renamed from index.js
- `src/pseudo-kit-react/tests/index.test.js` — 14 unit tests (new)
- `src/pseudo-kit-react/tests/setup.js` — IS_REACT_ACT_ENVIRONMENT flag
- `src/pseudo-kit-react/vitest.config.js` — new config with react plugin + jsdom
- `src/pseudo-kit-react/package.json` — version 0.3.0, devDeps: vitest, jsdom, @vitejs/plugin-react

**Notable decisions:**
- Used React 18 `createRoot` + `act` from `react` (not deprecated react-dom/test-utils) directly — no @testing-library/react required
- `_nameFromUrl` is tested indirectly via `useComponent` register spy assertion
