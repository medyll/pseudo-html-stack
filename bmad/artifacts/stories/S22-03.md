# S22-03 — FIX-PKG-03: peerDependencies → `>=1.0.0`

**Priority:** Low  
**Points:** 1

## Problem

Sub-packages declare `peerDependencies: { "pseudo-stack": ">=0.1.0" }`. Root is now `1.0.0`, so lower bound should be honest.

## Acceptance Criteria

1. `src/pseudo-canvas/package.json`: `"pseudo-stack": ">=1.0.0"`
2. `src/pseudo-assets/package.json`: `"pseudo-stack": ">=1.0.0"`

## Files to touch

- `src/pseudo-canvas/package.json`
- `src/pseudo-assets/package.json`
